[options]
; This is the password that allows database operations:
admin_passwd = Enterprise@2025
db_host = pgbouncer
db_port = 6432
db_user = odoo
db_password = odoo19@2025
addons_path = /mnt/enterprise-addons,/usr/lib/python3/dist-packages/odoo/addons

; Performance settings (optimized for Enterprise)
workers = 6
max_cron_threads = 4
limit_memory_hard = 4294967296
limit_memory_soft = 3221225472
limit_request = 8192
limit_time_cpu = 900
limit_time_real = 1800

; Database connection settings
db_maxconn = 64
db_template = template0

; Logging configuration
log_level = info
log_handler = :INFO

; Security settings
list_db = True
proxy_mode = True

; Server settings
http_enable = True
http_interface = 0.0.0.0
http_port = 8069

; Database settings
unaccent = True

; Demo data (disable in production)
without_demo = True

; Data directory
data_dir = /var/lib/odoo
